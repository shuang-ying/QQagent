# ============================================================
# API Key 模板
#
# 用法：把本文件复制成 .env（同目录），填入你自己的 Key。
#   copy .env.example .env
#
# 为什么不直接写在 providers.yaml 里：
#   .env 已在 .gitignore 中，不会被误提交；providers.yaml 会。
#
# 变量名怎么定：在 providers.yaml / providers.local.yaml 里，
# 给某个供应商写 apiKeyEnv: MY_KEY，这里就写 MY_KEY=sk-xxxx
# ============================================================

# 例：某个 OpenAI 兼容中转站
# MY_RELAY_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 例：DeepSeek 官方
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 如果 Key 已经直接写在 config/providers.local.yaml 的 apiKey 字段里，
# 那本文件可以留空不用。
